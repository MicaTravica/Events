import { TicketBuyReservation } from './ticket.buy.reservation.model';

describe('Ticket', () => {
  it('should create an instance', () => {
    expect(new TicketBuyReservation()).toBeTruthy();
  });
});
