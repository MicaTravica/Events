language: java
jdk:
  - openjdk8
# da bilda samo kad se pushne na master
branches:
  only:
    - master
install:
  - cd ./events
  - ls -la 
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:  
  - mvn compile
  - ls -la
  # - mvn test -B



